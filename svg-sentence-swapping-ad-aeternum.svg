<svg version="1.1"
     viewBox="0 0 600 50"
     width="100%" height="100%"
     xmlns="http://www.w3.org/2000/svg">

/* Adjust the viewBox="0 0 600 50" values above, if the actual non-testing sentences end up being much longer and need more width or height spacing. */

  <style>

    /* Assign animations to each sentence layer. */
    .sentence-one {
      animation: swapFirst 4s infinite;
    }
    
    .sentence-two {
      animation: swapSecond 4s infinite;
    }

    /* First sentence:  visible from 0-50%, fades out, and ends as invisible from 50-100%. */
    @keyframes swapFirst {
      0%, 45% {
        opacity: 1; visibility: visible;
      }
      50%, 95% {
        opacity: 0; visibility: hidden;
      }
      100% {
        opacity: 1; visibility: visible;
      }
    }

    /* Second sentence:  invisible from 0-50%, fades in, and ends as visible from 50-100%. */
    @keyframes swapSecond {
      0%, 45% {
        opacity: 0; visibility: hidden;
      }
      50%, 95% {
        opacity: 1; visibility: visible;
      }
      100% {
        opacity: 0; visibility: hidden;
      }
    }

  </style>



  <!-- The sentences are assigned the exact same coordinates, so they _SHOULD_ occupy the same space -->

  <text x="10" y="30" class="sentence-one">
    This is the first sentence.
  </text>

  <text x="10" y="30" class="sentence-two">
    This is the alternative sentence.
  </text>

</svg>
